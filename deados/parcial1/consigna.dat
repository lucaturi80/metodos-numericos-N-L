grupo 6

problema 1
a) Escriba un programa en FORTRAN utilizando buenas prácticas de programación y en doble precisión
 que permita obtener una aproximación numérica a una raíz de cierta función f(z) implementando los métodos 
 de bisección, secante y Newton, generando las salidas de pantalla y los archivos que se indican más adelante.

El programa principal debe llamarse p1.f90 y usted deberá crear además tres módulos:

⚫ de precisión, llamado precision1.f90,

⚫ de los métodos implementado, llamado metodos1.f90,

⚫ de las funciones, llamado funciones1.f90.

Los cuatro archivos con terminación. f90 (el programa principal y los tres módulos) deberán ser entregados.

Los datos de entrada deben inicializarse en la parte de definición de variables del programa principal y serán los siguientes:

• las aproximaciones iniciales a, b a los límites del intervalo, y to a la raíz buscada, según corresponda,

⚫ la tolerancia en x, tol,

⚫ la tolerancia en y, toly,

⚫ el número máximo de iteraciones mazite

El algoritmo debe detenerse cuando el error relativo en z sea menor que tol, y el error absoluto en y sea menor a fol, o cuando el número de iteraciones alcance el valor mazite.

El programa debe escribir en pantalla los siguientes valores finales:

⚫el número N de iteraciones realizadas,

el valor de la aproximación final zw.

el valor de la función en la aproximación final (ZN)

el error relativo en z final (aproximado)

y el error abstoluto en y final (aproximado)

Si no converge en mazite iteraciones debe parar y dar una señal de aviso. Además la rutina debe grabar en un archivo, para cada iteración, los siguientes valores:

el número de la iteración n,

la n-ésima aproximación In

la función f en la n-ésima aproximación f(x)

la estimación del error relativo en z, In-In-11/12), o bien eb-al/la+bl. según corresponda,

⚫ y el error absoluto en y, en = f(z),

donde los números reales deben representarse en notación exponencial con 9 cifras significa- tivas. Estos datos deben guardarse en un archivo dat y entregarse.

5) Grafique, utilizando Gnuplot y guarde en un archivo PDF llamado pib. pdf, la función a la cual deseamos encontrarle la raiz:

f(x)=cos(x²-0,5)

(1)

en el intervalo (0, 2). Para ello realice un script llamado plb.gp. Ambos archivos deben ser entregados.

c) Con el programa del punto a), encuentre una estimación a la raíz de la función (1), usando los siguentes valores iniciales

a=1.0 y b = 1.6 para el método de la bisección,

2o 1.3 para el método de Newton, y

S01.3 y 81 1.4 para el método de la secante.

En todos los casos se deber usar tol, 10-7, tol, 10-8 y mazite 100. Llame los archivos de datos generados p1_biseccion.dat, p1_secante. dat y p1_newton. dat, los cuales deben ser entregados.

) Grafique, utilizando Gnuplot, y guarde en un archivo PDF llamado p1d. pdf, el error relativo en z, En en función del número de iteración n, para los tres métodos, con escala lineal en z y logarítmica en y. Para ello realice un script llamado p1d.gp. Ambos archivos deben ser entregados.